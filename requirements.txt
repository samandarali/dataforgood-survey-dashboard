# ── Core dashboard ────────────────────────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.18.0
scipy>=1.12.0
statsmodels>=0.14.0


# ── Semantic / NLP (Tab 3 — Open-Ended Response Analysis) ────────────────────
sentence-transformers>=2.7.0   # sentence embeddings (all-MiniLM-L6-v2)
bertopic>=0.16.0               # topic modelling
umap-learn>=0.5.6              # dimensionality reduction (UMAP)
hdbscan>=0.8.38                # density-based clustering
scikit-learn>=1.4.0            # CountVectorizer, PCA fallback